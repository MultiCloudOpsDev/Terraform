ami_id = "ami-07860a2d7eb515d9a"    #add required ami id
instance_type = "t3.small"           #add required instance type


# Note: If your variable file has a custom name (e.g., test.tfvars), use:
#   terraform plan -var-file="test.tfvars"
#   terraform apply -var-file="test.tfvars"
# If your file is named 'terraform.tfvars', you don’t need to specify it — Terraform uses it automatically.
